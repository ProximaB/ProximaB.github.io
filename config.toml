# The URL the site will be built for
base_url = "https://proximab.github.io/"

# local testing
# base_url = "http://192.168.100.14:8080/"
# zola serve -p 8080 -i 192.168.100.14 -u 192.168.100.14

# Theme
theme = "float"

# The site title and description; used in feeds by default.
title = "proximab"
description = "Go, C#, Typescript, Python, Rust"

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
#highlight_code = true

# When set to "true", a feed is automatically generated.
generate_feed = true

# The filename to use for the feed. Used as the template filename, too.
# Defaults to "atom.xml", which has a builtin template that renders an Atom 1.0 feed.
# There is also a builtin template "rss.xml" that renders an RSS 2.0 feed.
# feed_filename = "atom.xml"

# The taxonomies to be rendered for the site and their configuration.
# Example:
#     taxonomies = [
#       {name = "tags", feed = true}, # each tag will have its own feed
#       {name = "tags", lang = "fr"}, # you can have taxonomies with the same name in multiple languages
#       {name = "categories", paginate_by = 5},  # 5 items per page for a term
#       {name = "authors"}, # Basic definition: no feed or pagination
#     ]
#
taxonomies = [
    {name = "tags", paginate_by = 10},
]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# When set to "true", a search index is built from the pages and section
# content for `default_language`.
build_search_index = true

[extra]
main_section = "blog"

copyright = "@proximab"

web_fonts = "<link rel='stylesheet' href='https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@500;700&display=swap'>"

google_analytics = true
google_analytics_id = "G-W8WCCFLXFF"

google_adsense = false
# google_adsense_id = "ca-pub-XXXXXXXXXXXXXXXX"

display_twitter_link = false
twitter_account = "@xxx"
twitter_url = "https://www.twitter.com/"

likecoin = false
# likecoin_name = "xxx"

utterances = true
utterances_repo = "ProximaB/ProximaB.github.io/"

[author]
name = "proximab"
homepage = "https://proximab.github.io/"